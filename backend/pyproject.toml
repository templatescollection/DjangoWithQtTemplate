[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.0",
    "django-rest-framework>=0.1.0",
    "gunicorn>=23.0.0",
]


[dependency-groups]
dev = [
    "mypy[faster-cache,reports]>=1.20.1",
    "pre-commit>=4.5.1",
    "pytest>=9.0.2",
    "pytest-django>=4.11.1",
    "pytest-django>=4.8.0",
    "pytest-cov>=4.0.0",
    "coverage>=7.0.0",
    "django-stubs>=5.0.0",
    "ruff>=0.14.14",
]
